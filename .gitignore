# Python
.venv/
__pycache__/
*.pyc

# Node
node_modules/
# package-lock.json wird BEWUSST versioniert: das Gate vergleicht die gebauten
# dist-Bytes: ohne Lockfile installiert CI andere Patch-Versionen und der
# Vergleich wird aus Toolchain- statt aus Staleness-Gründen rot.

# OS / IDE / local agent config
.DS_Store
.idea/
.ai/
.claude/settings.local.json
CLAUDE.local.md

# Eval-Artefakte: aus cases.yaml + dem laufenden Server erzeugt. Versioniert
# waeren sie am Tag nach der naechsten Beschreibungsaenderung falsch.
evals/tool-routing/tools.json
evals/tool-routing/promptfooconfig.yaml
